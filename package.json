{
  "name": "ccrelay",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "packages/shared",
    "packages/relay-server",
    "packages/worker-daemon",
    "packages/master-mcp"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "clean": "rm -rf packages/*/dist"
  },
  "engines": {
    "node": ">=20"
  }
}
